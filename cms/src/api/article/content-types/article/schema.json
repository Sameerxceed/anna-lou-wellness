{
  "kind": "collectionType",
  "collectionName": "articles",
  "info": {
    "singularName": "article",
    "pluralName": "articles",
    "displayName": "Story · Article",
    "description": "Editorial articles for Reset Stories, Life, Love & Relationships, Work & Money"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true
    },
    "excerpt": {
      "type": "text"
    },
    "body": {
      "type": "richtext"
    },
    "hero_image": {
      "type": "media",
      "multiple": false,
      "allowedTypes": [
        "images"
      ]
    },
    "category": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::article-category.article-category",
      "inversedBy": "articles"
    },
    "author": {
      "type": "string",
      "default": "Anna Lou"
    },
    "reading_time": {
      "type": "string"
    },
    "is_featured": {
      "type": "boolean",
      "default": false,
      "description": "Big banner article at the top of the homepage editorial section. Only one article should have this ticked at a time — most recent wins if multiple."
    },
    "is_homepage_pinned": {
      "type": "boolean",
      "default": false,
      "description": "Pin this article to the 3-card grid on the homepage. Up to 3 pinned articles show. If fewer than 3 are pinned, the rest fill with most-recent unpinned articles. Use 'homepage_pin_order' to control which pinned ones come first."
    },
    "homepage_pin_order": {
      "type": "integer",
      "default": 0,
      "description": "Order among pinned articles (lower number = appears first). Only used when 'is_homepage_pinned' is ticked. Leave at 0 if order does not matter."
    },
    "is_free": {
      "type": "boolean",
      "default": true,
      "description": "If true, shows on website. If false, Substack paid only."
    },
    "substack_canonical_url": {
      "type": "string",
      "description": "Canonical URL on Substack for the 12 free posts"
    },
    "seo_title": {
      "type": "string",
      "description": "Optional SEO title override (60 chars max). Leave blank to use the page/item name. Shown in Google search results and browser tabs."
    },
    "seo_description": {
      "type": "text",
      "description": "Short summary for Google and ChatGPT (1-2 sentences, ~155 chars). Write what someone searching for this would actually type. Use the 'Generate SEO' button if available, then edit."
    },
    "related_products": {
      "type": "relation",
      "relation": "manyToMany",
      "target": "api::product.product"
    },
    "shop_tags": {
      "type": "component",
      "repeatable": true,
      "component": "article.shop-tag",
      "description": "Optional 'Shop the story' photos shown below the article. Each photo can link to a product with a hover tag ('Anna is wearing the [piece]'). Use sparingly — only the photos where the product is central to the story. No inline shop links in the body."
    }
  }
}
