{
  "kind": "collectionType",
  "collectionName": "programmes",
  "info": {
    "singularName": "programme",
    "pluralName": "programmes",
    "displayName": "Work · Programme",
    "description": "One entry per coaching offer or 1:1 session (The Reset, Signal, Founder Reset, etc.). The slug must match the URL path."
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "description": "Programme name as it appears on the page (e.g. 'The Reset.' or 'Founder Reset')."
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true,
      "description": "URL slug. Must match the route — e.g. 'the-reset', 'signal', 'founder-reset'. Strapi auto-generates from title but you can edit."
    },
    "tagline": {
      "type": "string",
      "description": "Short italic line under the title. One sentence."
    },
    "accentColour": {
      "type": "string",
      "default": "#F280AA",
      "description": "Brand colour hex. Common options: pink #F280AA, blue #7BAFDD, gold #FAA21B, mint #5DCAA5, plum #6E3A5A."
    },
    "heroImage": {
      "type": "media",
      "multiple": false,
      "allowedTypes": [
        "images"
      ],
      "description": "Image at the top of the programme page. Portrait orientation works best."
    },
    "intro": {
      "type": "text",
      "description": "Main body. Separate paragraphs with a BLANK LINE between them. Each blank line creates a new paragraph on the page."
    },
    "whatsIncludedLabel": {
      "type": "string",
      "default": "What's included",
      "description": "Heading for the bullet-point section."
    },
    "whatsIncludedItems": {
      "type": "text",
      "description": "One bullet per line. Each line becomes a separate bullet point."
    },
    "approachLabel": {
      "type": "string",
      "default": "The approach",
      "description": "Optional — heading for a second info section."
    },
    "approachBody": {
      "type": "text",
      "description": "Optional — second info section body."
    },
    "outcomesLabel": {
      "type": "string",
      "default": "What changes",
      "description": "Optional — heading for a third info section."
    },
    "outcomesBody": {
      "type": "text",
      "description": "Optional — third info section body."
    },
    "pricingLabel": {
      "type": "string",
      "default": "Investment",
      "description": "Heading for the pricing block."
    },
    "pricingBody": {
      "type": "text",
      "description": "Pricing details. e.g. '£1,500. Paid in full or two instalments of £750.'"
    },
    "ctaLabel": {
      "type": "string",
      "default": "Book a 15-minute 1 to 1 chat",
      "description": "Bottom button text."
    },
    "ctaUrl": {
      "type": "string",
      "default": "/contact",
      "description": "Where the bottom button links to. Paste your Calendly link here (e.g. https://calendly.com/anna-annalouoflondon/discovery-call) and it will open as a popup so the customer stays on the site. Any other URL opens normally. IGNORED when `pricePence` is set — the button then goes to Stripe checkout using pricePence + postCheckoutCalendlyUrl."
    },
    "postCheckoutCalendlyUrl": {
      "type": "string",
      "description": "Where to send the customer AFTER a successful Stripe payment. Paste the Calendly URL for booking the first session (e.g. https://calendly.com/anna-annalouoflondon/the-reset-first-session). If blank, they land on the generic thank-you page instead. Only used when the programme has a `pricePence` set — free programmes use `ctaUrl` directly."
    },
    "displayOrder": {
      "type": "integer",
      "default": 100,
      "description": "Lower numbers show first in any list. Default 100."
    },
    "stagesList": {
      "type": "text",
      "description": "Optional. For programmes shown as 3+ stages (Recovery / Untangle-Unbind-Unbound). One stage per line. Format: 'Label|Title|Body description'. Example: 'Month One|Untangle.|We identify where the patterns live in the body.'. Leave blank if not needed."
    },
    "pricePence": {
      "type": "integer",
      "default": 0,
      "description": "Optional. Price in pence (e.g. 150000 = £1,500). Leave 0 or blank for 'by enquiry' programmes — the Book button won't show a price button."
    },
    "currency": {
      "type": "string",
      "default": "gbp",
      "description": "Three-letter ISO currency code, lowercase."
    },
    "isRecurring": {
      "type": "boolean",
      "default": false,
      "description": "Tick only if billed monthly/yearly. Leave unchecked for one-off programme payment."
    },
    "recurringInterval": {
      "type": "enumeration",
      "enum": [
        "month",
        "year"
      ],
      "description": "Only used when 'isRecurring' is on."
    },
    "mailchimpTag": {
      "type": "string",
      "description": "Tag attached on successful payment. Must match a Mailchimp tag name EXACTLY (case-sensitive). Examples: 'The Reset (6-week)', 'Signal (12-week)', 'Signal & Build (founders)'."
    },
    "grantsResetRoomAccess": {
      "type": "boolean",
      "default": false,
      "description": "Leave OFF for normal programmes. Only the Reset Room membership should have this on."
    },
    "grantsRegulatedAccess": {
      "type": "boolean",
      "default": false,
      "description": "Turn ON for the REGULATED programme entry only. When ticked, a Stripe purchase grants the buyer access to the course at /the-work/regulated/access. Leave OFF for every other programme."
    },
    "pwycOptions": {
      "type": "string",
      "description": "PAY-WHAT-YOU-CAN. Comma-separated list of amounts in pounds. Example: '50, 100, 200, 500'. When set, the sales page shows a dropdown so buyers choose how much they want to pay instead of one fixed price. Leave blank for a normal fixed-price programme (uses pricePence)."
    },
    "pwycDefault": {
      "type": "integer",
      "description": "PAY-WHAT-YOU-CAN. Which amount is pre-selected in the dropdown (in pounds). Must be one of the values in pwycOptions. Example: 100."
    },
    "pwycLabel": {
      "type": "string",
      "default": "Choose your amount",
      "description": "PAY-WHAT-YOU-CAN. Label above the price dropdown. Only shown when pwycOptions is set."
    },
    "seoTitle": {
      "type": "string",
      "description": "Optional SEO override. Leave blank to use the title."
    },
    "seoDescription": {
      "type": "text",
      "description": "Optional SEO description for Google. Keep under 160 characters."
    },
    "upsells": {
      "type": "component",
      "repeatable": true,
      "component": "shared.upsell-reference",
      "max": 3,
      "description": "Up to 3 'next step' cards shown at the bottom of this programme page. Anna's 8 Jun mapping: each page points the reader to the most relevant next offerings. Leave empty to hide the upsell block entirely (use this for B2B pages like Corporate Workshops)."
    }
  }
}
