{
  "kind": "singleType",
  "collectionName": "reset_stories_pages",
  "info": {
    "singularName": "reset-stories-page",
    "pluralName": "reset-stories-pages",
    "displayName": "Reset Stories",
    "description": "Landing page content for the Reset Stories section on the main menu."
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "kicker": {
      "type": "string",
      "default": "Reset Stories",
      "description": "Small uppercase label above the title."
    },
    "title": {
      "type": "string",
      "default": "Come back to yourself.",
      "description": "Big page headline."
    },
    "intro": {
      "type": "richtext",
      "description": "LEGACY — hidden. Edit intro_v2 instead.",
      "pluginOptions": {
        "content-manager": { "visible": false },
        "content-type-builder": { "visible": true }
      }
    },
    "intro_v2": {
      "type": "blocks",
      "description": "Intro paragraph below the title. Select text and click the link icon to add a hyperlink — a popup will ask you for the URL."
    },
    "hero_image": {
      "type": "media",
      "multiple": false,
      "allowedTypes": [
        "images"
      ],
      "description": "Optional hero image at the top of the section."
    },
    "kicker_colour": {
      "type": "string",
      "default": "#6E3A5A",
      "description": "Hex colour for the kicker label. Default is the brand colour for this section."
    },
    "seo_title": {
      "type": "string",
      "description": "Optional SEO title override (60 chars max). Leave blank to use the page/item name. Shown in Google search results and browser tabs."
    },
    "seo_description": {
      "type": "text",
      "description": "Short summary for Google and ChatGPT (1-2 sentences, ~155 chars). Write what someone searching for this would actually type. Fills in automatically when you Save — leave blank unless you want to override"
    },
    "upsells": {
      "type": "component",
      "repeatable": true,
      "component": "shared.upsell-reference",
      "max": 3,
      "description": "Up to 3 'next step' cards shown at the bottom of this page. Each card has an image + label + short blurb + link. Leave empty to hide the upsell block entirely. Example use: on the About page, point to the Reset Room and Reset Letters."
    }
  }
}
