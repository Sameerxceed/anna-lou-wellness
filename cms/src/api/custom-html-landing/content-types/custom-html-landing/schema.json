{
  "kind": "collectionType",
  "collectionName": "custom_html_landings",
  "info": {
    "singularName": "custom-html-landing",
    "pluralName": "custom-html-landings",
    "displayName": "Custom HTML Landing",
    "description": "Standalone landing pages built from raw HTML — for competitions, influencer collabs, one-off campaigns. Renders inside a sandboxed iframe so pasted HTML/CSS/JS just works without touching the rest of the site."
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "description": "Public page title. Shown in the browser tab and on social shares."
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true,
      "description": "URL path after /campaigns/. Lowercase, hyphens-not-spaces (e.g. 'feral-and-free' becomes /campaigns/feral-and-free). Auto-fills from Title the FIRST time you type the title — if it shows 'custom-html-landing' or something wrong, press the circular refresh icon at the right end of this field to regenerate from Title. Or just type it manually. Two special reserved slugs: use 'reset-letters' to override the /reset-letters holding page, or use the same slug as an Experience entry to render your HTML on that experience's detail page."
    },
    "raw_html": {
      "type": "text",
      "required": true,
      "description": "Paste the ENTIRE HTML file here (from <!DOCTYPE html> all the way to </html>, or just the body if you prefer). It renders in a fully sandboxed iframe, so your CSS won't clash with the site's styles and your form/script tags DO run. Update this field to update the live page — save and it's live in seconds."
    },
    "iframe_height": {
      "type": "string",
      "default": "auto",
      "description": "Height of the iframe on the page. 'auto' (default) grows to fit the content. Or set a fixed value like '900px', '100vh', '1400px'. Use 'auto' unless the page mis-sizes on mobile."
    },
    "show_site_nav": {
      "type": "boolean",
      "default": true,
      "description": "Tick to show the standard Anna Lou Wellness nav at the top of the page. Untick for a bare, distraction-free landing (recommended for competitions and paid ads)."
    },
    "hero_image": {
      "type": "media",
      "multiple": false,
      "allowedTypes": ["images"],
      "description": "Social share image (Facebook, Twitter, WhatsApp preview). 1200×630 landscape. Optional but recommended — makes the link look inviting when someone shares it."
    },
    "images": {
      "type": "media",
      "multiple": true,
      "allowedTypes": ["images"],
      "description": "Upload up to ~10 images used inside your HTML. In the HTML body use tokens like {{image_1}} for the first image, {{image_2}} for the second, etc. — the site replaces them with the correct URL. Zero URL building. Example: <img src=\"{{image_1}}\" alt=\"...\">"
    },
    "focus_keyword": {
      "type": "string",
      "description": "The ONE phrase you most want this campaign page to rank for on Google. Example: 'anna lou toni nagy feral and free'. Feeds into the auto-generated SEO title + description on Save."
    },
    "supporting_keywords": {
      "type": "text",
      "description": "Comma-separated related search phrases. Used alongside the focus keyword when the site auto-writes SEO copy."
    },
    "tags": {
      "type": "string",
      "description": "Comma-separated tags for filtering + internal tracking. Optional."
    },
    "seo_title": {
      "type": "string",
      "description": "Optional SEO title override (60 chars max). Leave blank to use the page title."
    },
    "seo_description": {
      "type": "text",
      "description": "Short summary for Google and social previews (~155 chars). Fills in automatically when you Save — leave blank unless you want to override."
    }
  }
}
