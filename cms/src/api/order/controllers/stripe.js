'use strict';

// Stub kept to satisfy any cached require paths. The real Stripe webhook
// handler lives on the Next.js side at web/app/api/stripe/webhook/route.ts.
// The legacy Strapi handler (€-currency receipts via the never-configured
// Strapi email plugin) has been removed.

module.exports = {};
