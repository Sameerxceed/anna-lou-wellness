'use strict';

// The real Stripe webhook lives on the Next.js side at /api/stripe/webhook
// (web/app/api/stripe/webhook/route.ts). Stripe dashboard endpoint points
// there, not here. The legacy Strapi-side handler is removed.

module.exports = { routes: [] };
